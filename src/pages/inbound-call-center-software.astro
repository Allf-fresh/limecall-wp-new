---
import Base from '../layouts/Base.astro';
import Breadcrumbs from '../components/seo/Breadcrumbs.astro';
import CrossLinks from '../components/CrossLinks.astro';
import { site } from '../site.config';

const seo = {
  title: `Inbound Call Center Software | LimeCall`,
  description: `LimeCall's inbound call center software helps you manage high call volumes, route calls intelligently, record conversations, and support customers from any device or location.`,
  canonical: `https://www.limecall.com/inbound-call-center-software/`,
};

const features = [
  { title: "Intelligent Call Routing", desc: "Route inbound calls to the right agent based on IVR selection, caller history, skills, or availability. Minimize transfers." },
  { title: "Call Queuing", desc: "Handle high call volumes with smart queuing. Give callers their estimated wait time and offer callback options instead of holding." },
  { title: "Mobile App Support", desc: "Agents can take inbound calls from the LimeCall mobile app on any smartphone — no desk phone required." },
  { title: "Call Recording & Monitoring", desc: "Record all inbound calls. Supervisors can listen live, whisper to agents, or barge into calls for quality assurance." },
  { title: "Real-Time Dashboard", desc: "Monitor live call volumes, agent status, queue lengths, and wait times in a real-time dashboard." },
  { title: "After-Call Work", desc: "Give agents time for after-call work — logging notes, updating CRM — before routing the next call." },
  { title: "IVR / Auto-Attendant", desc: "Set up multi-level IVR menus to qualify and route callers automatically — 24/7, without a human receptionist." },
  { title: "CRM Integration", desc: "Every inbound call auto-creates or updates a CRM record with caller details, call recording, and agent notes." },
];
---

<Base {...seo}>
  <!-- Hero -->
  <section class="border-b border-dark-800 bg-gradient-to-b from-dark-900 to-dark-950 pt-14 pb-10 sm:pt-20 sm:pb-14">
    <div class="mx-auto max-w-3xl px-4 text-center sm:px-6 lg:px-8">
      <Breadcrumbs items={[{ name: "Inbound Call Center Software", url: seo.canonical }]} />
      <div class="mt-4 inline-flex items-center gap-2 rounded-full border border-lime-500/30 bg-lime-500/10 px-4 py-1.5 text-sm font-medium text-lime-400">
        Inbound Call Center Software
      </div>
      <h1 class="mt-4 text-4xl font-extrabold leading-tight text-white sm:text-5xl">
        Inbound Call Center Software <span class="text-lime-400">Built for Modern Teams</span>
      </h1>
      <p class="mx-auto mt-5 max-w-xl text-lg text-dark-300 leading-relaxed">
        Handle high inbound call volumes with confidence. LimeCall gives your team intelligent routing, real-time monitoring, mobile support, and full analytics — in one cloud platform.
      </p>
      <div class="mt-8 flex flex-wrap justify-center gap-4">
        <a href={site.cta.href} class="rounded-xl bg-lime-500 px-8 py-3.5 text-sm font-bold text-dark-950 transition hover:bg-lime-400 shadow-lg">
          Start Free Trial →
        </a>
        <a href="/pricing/" class="rounded-xl border border-dark-700 bg-dark-800 px-8 py-3.5 text-sm font-bold text-white transition hover:border-lime-500/40">
          View Pricing
        </a>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="py-16 sm:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <h2 class="mb-12 text-center text-3xl font-bold text-white">Everything in your inbound call center</h2>
      <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
        {features.map(f => (
          <div class="rounded-xl border border-dark-800 bg-dark-900/60 p-6">
            <h3 class="mb-2 font-semibold text-white">{f.title}</h3>
            <p class="text-sm text-dark-400 leading-relaxed">{f.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Why Cloud-Based -->
  <section class="border-t border-dark-800 py-16 sm:py-20 bg-dark-900/30">
    <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
      <h2 class="mb-8 text-center text-2xl font-bold text-white">Why cloud-based inbound call center software?</h2>
      <div class="space-y-4 text-dark-300 leading-relaxed">
        <p>Traditional call centers require expensive hardware, physical locations, and complex IT infrastructure. Cloud-based inbound call center software eliminates all of that.</p>
        <p>With LimeCall, your agents can take calls from anywhere — home, office, or on the go. New agents are onboarded in minutes, not days. Scaling up or down is instant.</p>
        <p>You pay only for what you use. No CapEx, no maintenance, no hardware failures. Just a modern, reliable inbound call platform that grows with your team.</p>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="border-t border-dark-800 py-16 bg-gradient-to-b from-dark-950 to-dark-900">
    <div class="mx-auto max-w-3xl px-4 text-center sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold text-white">Set up your cloud call center in minutes</h2>
      <p class="mt-4 text-dark-400">No hardware, no IT team, no long contracts. Start your LimeCall free trial today.</p>
      <a href={site.cta.href} class="mt-8 inline-block rounded-xl bg-lime-500 px-10 py-4 text-base font-bold text-dark-950 transition hover:bg-lime-400">
        Start Free Trial →
      </a>
    </div>
  </section>

  <div class="py-8">
    <CrossLinks
      heading="Explore More"
      links={[
        { href: '/customer-support-software/', title: 'Customer Support Software', desc: 'Full customer support platform.' },
        { href: '/callbackautomation/', title: 'Callback Automation', desc: 'Automate customer callbacks.' },
        { href: '/call-tracking/', title: 'Call Tracking', desc: 'Track which campaigns drive calls.' },
        { href: '/pricing/', title: 'Pricing', desc: 'Simple, transparent pricing.' },
      ]}
    />
  </div>
</Base>
