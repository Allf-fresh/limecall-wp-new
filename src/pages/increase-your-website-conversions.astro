---
import Base from '../layouts/Base.astro';
import Breadcrumbs from '../components/seo/Breadcrumbs.astro';
import CrossLinks from '../components/CrossLinks.astro';
import JsonLd from '../components/seo/JsonLd.astro';
import { site } from '../site.config';

const icon = "📊";

const seo = {
  title: `Increase Website Conversions with LimeCall | CRO`,
  description: `Stop website visitors from bouncing. LimeCall's instant callback widget converts more of your existing traffic into leads and customers — no extra ad spend required.`,
  canonical: `https://www.limecall.com/increase-your-website-conversions/`,
};

const useCases = [
  { title: "Convert More Visitors Without More Traffic", desc: "Most CRO strategies focus on copy and design. LimeCall tackles the #1 conversion killer — slow follow-up. By connecting a human to every high-intent visitor in under 28 seconds, you get more from the traffic you already have." },
  { title: "Reduce Bounce Rate on High-Intent Pages", desc: "Pricing pages, demo pages, and product pages have the highest intent but also the highest drop-off. Place LimeCall's callback widget on these pages to intercept visitors at the critical moment before they bounce." },
  { title: "Exit-Intent Callback Recovery", desc: "When a visitor moves to close the tab or navigate away, trigger an exit-intent callback offer. A real human conversation at the exit point converts browsers who would otherwise be lost forever." },
  { title: "A/B Test Callback Placement & Messaging", desc: "LimeCall integrates with your analytics stack so you can test which pages, messaging, and widget placements drive the highest conversion rates. Data-driven CRO, powered by real human conversations." },
];

const stats = [
  { value: "10–12x", label: "Higher conversion than forms" },
  { value: "72%", label: "Visitors leave without converting" },
  { value: "28s", label: "Avg. callback time" },
  { value: "5 min", label: "Setup time" },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: [
    {
      '@type': 'Question',
      name: 'How do I increase website conversions?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'The most effective way to increase website conversions is to reduce the time between a visitor showing interest and a human making contact. Adding a callback widget like LimeCall to your high-intent pages ensures every interested visitor can connect with your team in under 28 seconds — dramatically improving conversion rates without spending more on ads.',
      },
    },
    {
      '@type': 'Question',
      name: 'What converts better than a contact form?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'Phone callbacks consistently outperform contact forms. Forms have average completion rates of 1–3% and require hours of follow-up. An instant callback widget like LimeCall connects a real human within 28 seconds of a visitor expressing interest, converting at 10–12x the rate of a form.',
      },
    },
    {
      '@type': 'Question',
      name: 'How much does a callback widget improve conversions?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'LimeCall customers typically see a 200–400% uplift in conversions from pages where the callback widget is placed, compared to form-only pages. The exact improvement depends on the industry, traffic quality, and current baseline conversion rate.',
      },
    },
    {
      '@type': 'Question',
      name: 'What is a good website conversion rate?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'Average website conversion rates vary by industry: SaaS and software average 3–5%, B2B services average 2–4%, and e-commerce averages 1–3%. With an instant callback widget, companies typically achieve 2–4x their baseline rate on pages where real-time conversation is offered.',
      },
    },
    {
      '@type': 'Question',
      name: 'How does LimeCall install on my website?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'LimeCall installs with a single JavaScript snippet added to your site — similar to Google Analytics or a chat widget. Setup takes under 5 minutes with no developer required. LimeCall also has native integrations for WordPress, Shopify, and most popular CMS platforms.',
      },
    },
  ],
};

const comparisonRows = [
  { label: "Avg. conversion rate", callback: "8–12%", form: "1–3%", chat: "3–6%" },
  { label: "Avg. response time", callback: "28 seconds", form: "4–24 hours", chat: "Varies (often bot)" },
  { label: "Human conversation", callback: "Always", form: "Delayed", chat: "Often automated" },
  { label: "Cost per lead", callback: "Low", form: "High (slow follow-up waste)", chat: "Medium" },
  { label: "Mobile friendly", callback: "Yes — calls phone", form: "Yes", chat: "Partial" },
];
---

<Base {...seo}>
  <JsonLd schema={faqSchema} />

  <!-- Hero -->
  <section class="border-b border-dark-800 bg-gradient-to-b from-dark-900 to-dark-950 pt-14 pb-10 sm:pt-20 sm:pb-14">
    <div class="mx-auto max-w-3xl px-4 text-center sm:px-6 lg:px-8">
      <Breadcrumbs items={[{ name: `Increase Website Conversions`, url: seo.canonical }]} />
      <div class="mt-4 text-5xl">{icon}</div>
      <h1 class="mt-3 text-4xl font-extrabold leading-tight text-white sm:text-5xl">
        <span class="text-lime-400">Increase Your Website</span> Conversions
      </h1>
      <p class="mx-auto mt-5 max-w-xl text-lg text-dark-300 leading-relaxed">
        Stop losing visitors to slow follow-up. LimeCall converts up to 300% more of your existing website traffic into leads and customers.
      </p>
      <div class="mt-8 flex flex-wrap justify-center gap-4">
        <a href={site.cta.href} class="rounded-xl bg-lime-500 px-8 py-3.5 text-sm font-bold text-dark-950 hover:bg-lime-400 transition shadow-lg">
          Start Free Trial →
        </a>
        <a href="/pricing/" class="rounded-xl border border-dark-700 bg-dark-800 px-8 py-3.5 text-sm font-bold text-white hover:border-lime-500/40 transition">
          View Pricing
        </a>
      </div>
    </div>
  </section>

  <!-- Stats -->
  <section class="border-b border-dark-800 bg-dark-900/50 py-10">
    <div class="mx-auto max-w-4xl px-4">
      <div class="grid grid-cols-2 gap-6 sm:grid-cols-4">
        {stats.map(s => (
          <div class="text-center">
            <div class="text-3xl font-extrabold text-lime-400">{s.value}</div>
            <div class="mt-1 text-sm text-dark-400">{s.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Use cases -->
  <section class="py-16 sm:py-20">
    <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
      <h2 class="mb-10 text-center text-2xl font-bold text-white">How LimeCall increases your conversion rate</h2>
      <div class="grid gap-6 sm:grid-cols-2">
        {useCases.map(uc => (
          <div class="rounded-xl border border-dark-800 bg-dark-900/60 p-6">
            <h3 class="mb-2 font-semibold text-white">{uc.title}</h3>
            <p class="text-sm text-dark-400 leading-relaxed">{uc.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- How it works -->
  <section class="border-t border-dark-800 py-16 sm:py-20 bg-dark-900/30">
    <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
      <h2 class="mb-10 text-center text-2xl font-bold text-white">How it works</h2>
      <div class="grid gap-8 sm:grid-cols-3">
        {[
          { step: "1", title: "Embed the widget", desc: "Add LimeCall's callback widget to your highest-converting pages in minutes. No developer required." },
          { step: "2", title: "Visitor requests callback", desc: "A high-intent visitor clicks the widget or is prompted by an exit-intent trigger and requests an instant callback." },
          { step: "3", title: "Conversion happens", desc: "Your team is connected to the visitor in under 28 seconds. A real conversation converts where forms and chatbots fail." },
        ].map(s => (
          <div class="text-center">
            <div class="mx-auto mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-lime-500/20 text-lg font-extrabold text-lime-400">{s.step}</div>
            <h3 class="mb-2 font-semibold text-white">{s.title}</h3>
            <p class="text-sm text-dark-400 leading-relaxed">{s.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Comparison table -->
  <section class="border-t border-dark-800 py-16 sm:py-20">
    <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">
      <h2 class="mb-8 text-center text-2xl font-bold text-white">Callback widget vs. contact form vs. live chat</h2>
      <div class="overflow-x-auto rounded-xl border border-dark-800">
        <table class="w-full text-sm">
          <thead>
            <tr class="border-b border-dark-800 bg-dark-900/80">
              <th class="px-5 py-4 text-left font-semibold text-dark-300"></th>
              <th class="px-5 py-4 text-center font-semibold text-lime-400">Callback Widget</th>
              <th class="px-5 py-4 text-center font-semibold text-dark-300">Contact Form</th>
              <th class="px-5 py-4 text-center font-semibold text-dark-300">Live Chat</th>
            </tr>
          </thead>
          <tbody>
            {comparisonRows.map((row, i) => (
              <tr class={`border-b border-dark-800 ${i % 2 === 0 ? 'bg-dark-900/40' : 'bg-dark-900/20'}`}>
                <td class="px-5 py-4 font-medium text-dark-300">{row.label}</td>
                <td class="px-5 py-4 text-center font-semibold text-lime-400">{row.callback}</td>
                <td class="px-5 py-4 text-center text-dark-400">{row.form}</td>
                <td class="px-5 py-4 text-center text-dark-400">{row.chat}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="border-t border-dark-800 py-16 sm:py-20 bg-dark-900/30">
    <div class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8">
      <h2 class="mb-10 text-center text-2xl font-bold text-white">Frequently asked questions</h2>
      <div class="space-y-4">
        {faqSchema.mainEntity.map(q => (
          <details class="group rounded-xl border border-dark-800 bg-dark-900/60 p-5">
            <summary class="flex cursor-pointer items-center justify-between font-semibold text-white list-none">
              {q.name}
              <span class="ml-4 shrink-0 text-lime-400 transition group-open:rotate-45">+</span>
            </summary>
            <p class="mt-4 text-sm text-dark-400 leading-relaxed">{q.acceptedAnswer.text}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="border-t border-dark-800 py-16 bg-gradient-to-b from-dark-950 to-dark-900">
    <div class="mx-auto max-w-2xl px-4 text-center">
      <h2 class="text-3xl font-bold text-white">Ready to double your website conversion rate?</h2>
      <p class="mt-4 text-dark-400">Set up in 5 minutes. No credit card required.</p>
      <a href={site.cta.href} class="mt-8 inline-block rounded-xl bg-lime-500 px-10 py-4 font-bold text-dark-950 hover:bg-lime-400 transition">
        Start Free →
      </a>
    </div>
  </section>

  <div class="py-8 mx-auto max-w-5xl px-4">
    <CrossLinks heading="More LimeCall Solutions" links={[
      { href: '/connect-with-visitors-instantly/', title: 'Connect Instantly', desc: 'Real-time visitor engagement tools.' },
      { href: '/generate-more-leads/', title: 'Generate More Leads', desc: 'Turn traffic into pipeline.' },
      { href: '/increase-sales/', title: 'Increase Sales', desc: 'Close more deals with faster follow-up.' },
      { href: '/pricing/', title: 'Pricing', desc: 'Simple, transparent plans.' },
    ]} />
  </div>
</Base>
